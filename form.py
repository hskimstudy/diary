from fastapi import FastAPI, Form

# app = FastAPI()

# @app.post("/users")
# async def create_user(A: int = Form(...), B: int = Form(...), C: int = Form(...), D: int = Form(...), E: int = Form(...)):
#     return {"A": A, "B": B, "C": C, "D": D, "E": E}